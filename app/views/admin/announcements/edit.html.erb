<h2 class="font-bold text-xl mb-8">Edit announcement 「<%= @announcement.title %>」 (id:<%= @announcement.id %>)</h2>
<div class="pr-8 mb-20">
  <%= form_with model: @announcement, url: admin_announcement_path(@announcement), method: :patch do |f| %>
    <%= f.hidden_field :event_id, value: @announcement.event.id %>
    <%= f.label :title, "announce title", class: "block text-lg font-bold mb-2" %>
    <%= f.text_field :title, class: "block rounded mb-4 w-[40rem]" %>
    <%= f.label :content, "announce content", class: "block text-lg font-bold mb-2" %>
    <%= f.rich_text_area :content, class: "block mb-4" %>
    <%= f.label :status, "announce content", class: "block text-lg font-bold mb-2" %>
    <%= f.select :status, Announcement.statuses, {}, class: "block rounded border-1 cursor-pointer disabled:bg-gray-200 disabled:cursor-not-allowed mb-8", disabled: @announcement.published? %>
    <%= f.submit class: "block border shadow border-gray-500 rounded px-4 py-2 cursor-pointer" %>
  <% end %>
</div>
