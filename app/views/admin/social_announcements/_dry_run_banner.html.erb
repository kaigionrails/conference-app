<% if Social.dry_run? %>
  <div class="py-3 px-5 mb-4 mr-8 border border-yellow-400 bg-yellow-50 rounded-sm">
    dry-run mode: external SNS APIs are not called. Posts are recorded with fake remote ids.
  </div>
<% end %>
